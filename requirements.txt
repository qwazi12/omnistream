# ── UI ────────────────────────────────────────────────────────────────────
customtkinter>=5.2.0

# ── Download engines ──────────────────────────────────────────────────────
yt-dlp>=2024.1.0              # pin to a recent year to avoid old API breakage
playwright>=1.40.0
TikTokApi>=6.0.0              # used by tiktok_engine.py

# ── HTTP / scraping ───────────────────────────────────────────────────────
requests>=2.31.0
fake-useragent>=1.4.0

# ── Google APIs ───────────────────────────────────────────────────────────
google-api-python-client>=2.100.0
google-auth-httplib2>=0.1.1
google-auth-oauthlib>=1.1.0
google-generativeai>=0.8.0    # used by ai_assistant.py and metadata_generator.py
gspread>=5.0.0

# ── Deprecated but still actively used in sheets_manager.py ───────────────
# TODO: migrate sheets_manager.py to google-auth + google-api-python-client
#       then remove this line.
oauth2client>=4.1.3

# ── Environment ───────────────────────────────────────────────────────────
python-dotenv>=1.0.0

# ── REMOVED (confirmed unused in all project source files) ────────────────
# pillow>=10.0.0   — no PIL import found anywhere in project .py files
# myjdapi>=0.1.0   — JDownloader invoked via subprocess/file, not Python API
